
name: Test the application
on: [pull_request]
env:
  IMAGE_TAG: nestjs-api
jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repository
        uses: actions/checkout@v3
 
      - run: echo "Hello github actions/checkout"