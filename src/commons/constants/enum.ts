export enum UserStatus {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
}

export enum ProfileStatus {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
}

export enum ProfileRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
}
